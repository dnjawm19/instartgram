language: java
jdk:
- openjdk11
branches:
  only:
  - master
cache:
  directories:
  - "$HOME/.m2/repository"
  - "$HOME/.gradle"
before_install:
- openssl aes-256-cbc -K $encrypted_279d18307899_key -iv $encrypted_279d18307899_iv
  -in aws.tar.enc -out aws.tar -d
- tar xvf aws.tar
- chmod +x gradlew
script: "./gradlew clean build"
notifications:
  email:
    recipients:
    - dnajwm1995@gmail.com
